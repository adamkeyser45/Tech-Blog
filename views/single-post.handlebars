{{> post-info post}}

{{#if loggedIn}}
<div class="card">
    <div class="card-body">
        <form class="comment-form">
            <div>
                <textarea name="comment-body" style="width: 50%; height: 100px"></textarea>
            </div>

            <div>
                <button type="submit" class="btn-outline-success">Leave a Comment!</button>
            </div>
        </form>        
    </div>
</div>

{{/if}}

{{> comments post.comments}}

{{#if loggedIn}}
<script src="/javascript/comment.js"></script>
{{/if}}