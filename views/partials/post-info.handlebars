<article class="post">
    <div class="title">
        <p>{{title}}</p>
    </div>
    <div class="meta">
        By {{user.username}} on
        {{created_at}}
        |
        <a href="/post/{{id}}"> Comment(s): {{comments.length}} </a>
    </div>
</article>
